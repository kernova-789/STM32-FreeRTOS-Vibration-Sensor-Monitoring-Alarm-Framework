#include "screen_driver.h"

#include "FreeRTOS.h"
#include "string.h"
#include "task.h"

#define SCREEN_DRIVER_SPI_TIMEOUT_MS 100U
#define SCREEN_DRIVER_GLYPH_WIDTH 5U
#define SCREEN_DRIVER_CHARACTER_WIDTH 6U
#define SCREEN_DRIVER_LARGE_GLYPH_WIDTH 8U
#define SCREEN_DRIVER_LARGE_GLYPH_HEIGHT 16U
#define SCREEN_DRIVER_LARGE_CHARACTER_WIDTH 8U
#define SCREEN_DRIVER_LARGE_ROW_COUNT (SCREEN_DRIVER_PAGE_COUNT / 2U)

typedef struct {
  char character;
  uint8_t columns[SCREEN_DRIVER_GLYPH_WIDTH];
} screen_glyph_t;

typedef struct {
  char character;
  uint8_t rows[SCREEN_DRIVER_LARGE_GLYPH_HEIGHT];
} screen_large_glyph_t;

/* 每列字模的最低位对应字符顶部像素。 */
static const screen_glyph_t screen_font[] = {
    {' ', {0x00, 0x00, 0x00, 0x00, 0x00}},
    {'(', {0x00, 0x1C, 0x22, 0x41, 0x00}},
    {')', {0x00, 0x41, 0x22, 0x1C, 0x00}},
    {'-', {0x08, 0x08, 0x08, 0x08, 0x08}},
    {'.', {0x00, 0x60, 0x60, 0x00, 0x00}},
    {'/', {0x20, 0x10, 0x08, 0x04, 0x02}},
    {'0', {0x3E, 0x51, 0x49, 0x45, 0x3E}},
    {'1', {0x00, 0x42, 0x7F, 0x40, 0x00}},
    {'2', {0x42, 0x61, 0x51, 0x49, 0x46}},
    {'3', {0x21, 0x41, 0x45, 0x4B, 0x31}},
    {'4', {0x18, 0x14, 0x12, 0x7F, 0x10}},
    {'5', {0x27, 0x45, 0x45, 0x45, 0x39}},
    {'6', {0x3C, 0x4A, 0x49, 0x49, 0x30}},
    {'7', {0x01, 0x71, 0x09, 0x05, 0x03}},
    {'8', {0x36, 0x49, 0x49, 0x49, 0x36}},
    {'9', {0x06, 0x49, 0x49, 0x29, 0x1E}},
    {':', {0x00, 0x36, 0x36, 0x00, 0x00}},
    {'?', {0x02, 0x01, 0x51, 0x09, 0x06}},
    {'+', {0x08, 0x08, 0x3E, 0x08, 0x08}},
    {'A', {0x7E, 0x11, 0x11, 0x11, 0x7E}},
    {'B', {0x7F, 0x49, 0x49, 0x49, 0x36}},
    {'C', {0x3E, 0x41, 0x41, 0x41, 0x22}},
    {'D', {0x7F, 0x41, 0x41, 0x22, 0x1C}},
    {'E', {0x7F, 0x49, 0x49, 0x49, 0x41}},
    {'F', {0x7F, 0x09, 0x09, 0x09, 0x01}},
    {'G', {0x3E, 0x41, 0x49, 0x49, 0x7A}},
    {'H', {0x7F, 0x08, 0x08, 0x08, 0x7F}},
    {'I', {0x00, 0x41, 0x7F, 0x41, 0x00}},
    {'J', {0x20, 0x40, 0x41, 0x3F, 0x01}},
    {'K', {0x7F, 0x08, 0x14, 0x22, 0x41}},
    {'L', {0x7F, 0x40, 0x40, 0x40, 0x40}},
    {'M', {0x7F, 0x02, 0x0C, 0x02, 0x7F}},
    {'N', {0x7F, 0x04, 0x08, 0x10, 0x7F}},
    {'O', {0x3E, 0x41, 0x41, 0x41, 0x3E}},
    {'P', {0x7F, 0x09, 0x09, 0x09, 0x06}},
    {'Q', {0x3E, 0x41, 0x51, 0x21, 0x5E}},
    {'R', {0x7F, 0x09, 0x19, 0x29, 0x46}},
    {'S', {0x46, 0x49, 0x49, 0x49, 0x31}},
    {'T', {0x01, 0x01, 0x7F, 0x01, 0x01}},
    {'U', {0x3F, 0x40, 0x40, 0x40, 0x3F}},
    {'V', {0x1F, 0x20, 0x40, 0x20, 0x1F}},
    {'W', {0x3F, 0x40, 0x38, 0x40, 0x3F}},
    {'X', {0x63, 0x14, 0x08, 0x14, 0x63}},
    {'Y', {0x07, 0x08, 0x70, 0x08, 0x07}},
    {'Z', {0x61, 0x51, 0x49, 0x45, 0x43}},
};

/*
 * 原生 8x16 字模。每个字节描述一行，最高位对应字符最左侧像素。
 * 字形直接按 8x16 分辨率设计，不再由 5x7 字模拉伸生成。
 */
static const screen_large_glyph_t screen_large_font[] = {
    {' ', {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
           0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00}},
    {'(', {0x00, 0x00, 0x0C, 0x18, 0x30, 0x30, 0x60, 0x60,
           0x60, 0x60, 0x30, 0x30, 0x18, 0x0C, 0x00, 0x00}},
    {')', {0x00, 0x00, 0x60, 0x30, 0x18, 0x18, 0x0C, 0x0C,
           0x0C, 0x0C, 0x18, 0x18, 0x30, 0x60, 0x00, 0x00}},
    {'-', {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x7E,
           0x7E, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00}},
    {'.', {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
           0x00, 0x00, 0x00, 0x00, 0x18, 0x18, 0x00, 0x00}},
    {'/', {0x00, 0x03, 0x03, 0x06, 0x0C, 0x0C, 0x18, 0x30,
           0x30, 0x60, 0xC0, 0xC0, 0x80, 0x00, 0x00, 0x00}},
    {'0', {0x00, 0x00, 0x3C, 0x66, 0xC3, 0xC3, 0xC7, 0xCB,
           0xD3, 0xE3, 0xC3, 0xC3, 0x66, 0x3C, 0x00, 0x00}},
    {'1', {0x00, 0x00, 0x18, 0x38, 0x78, 0x18, 0x18, 0x18,
           0x18, 0x18, 0x18, 0x18, 0x18, 0x7E, 0x00, 0x00}},
    {'2', {0x00, 0x00, 0x7E, 0xC3, 0x03, 0x03, 0x06, 0x0C,
           0x18, 0x30, 0x60, 0xC0, 0xC3, 0xFF, 0x00, 0x00}},
    {'3', {0x00, 0x00, 0x7E, 0xC3, 0x03, 0x03, 0x06, 0x1C,
           0x06, 0x03, 0x03, 0x03, 0xC3, 0x7E, 0x00, 0x00}},
    {'4', {0x00, 0x00, 0x06, 0x0E, 0x1E, 0x36, 0x66, 0xC6,
           0xFF, 0x06, 0x06, 0x06, 0x06, 0x0F, 0x00, 0x00}},
    {'5', {0x00, 0x00, 0xFF, 0xC0, 0xC0, 0xC0, 0xFC, 0xC6,
           0x03, 0x03, 0x03, 0x03, 0xC6, 0x7C, 0x00, 0x00}},
    {'6', {0x00, 0x00, 0x3C, 0x66, 0xC0, 0xC0, 0xDC, 0xE6,
           0xC3, 0xC3, 0xC3, 0xC3, 0x66, 0x3C, 0x00, 0x00}},
    {'7', {0x00, 0x00, 0xFF, 0xC3, 0x03, 0x06, 0x06, 0x0C,
           0x0C, 0x18, 0x18, 0x30, 0x30, 0x30, 0x00, 0x00}},
    {'8', {0x00, 0x00, 0x3C, 0x66, 0xC3, 0xC3, 0x66, 0x3C,
           0x66, 0xC3, 0xC3, 0xC3, 0x66, 0x3C, 0x00, 0x00}},
    {'9', {0x00, 0x00, 0x3C, 0x66, 0xC3, 0xC3, 0xC3, 0x67,
           0x3B, 0x03, 0x03, 0x03, 0x66, 0x3C, 0x00, 0x00}},
    {':', {0x00, 0x00, 0x00, 0x00, 0x18, 0x18, 0x00, 0x00,
           0x00, 0x00, 0x18, 0x18, 0x00, 0x00, 0x00, 0x00}},
    {'?', {0x00, 0x00, 0x7E, 0xC3, 0x03, 0x06, 0x0C, 0x18,
           0x18, 0x00, 0x18, 0x18, 0x00, 0x00, 0x00, 0x00}},
    {'A', {0x00, 0x00, 0x18, 0x3C, 0x66, 0xC3, 0xC3, 0xC3,
           0xFF, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0x00, 0x00}},
    {'B', {0x00, 0x00, 0xFC, 0x66, 0x63, 0x63, 0x66, 0x7C,
           0x66, 0x63, 0x63, 0x63, 0x66, 0xFC, 0x00, 0x00}},
    {'C', {0x00, 0x00, 0x3E, 0x63, 0xC3, 0xC0, 0xC0, 0xC0,
           0xC0, 0xC0, 0xC0, 0xC3, 0x63, 0x3E, 0x00, 0x00}},
    {'D', {0x00, 0x00, 0xFC, 0x66, 0x63, 0x63, 0x63, 0x63,
           0x63, 0x63, 0x63, 0x63, 0x66, 0xFC, 0x00, 0x00}},
    {'E', {0x00, 0x00, 0xFF, 0x63, 0x60, 0x60, 0x64, 0x7C,
           0x64, 0x60, 0x60, 0x60, 0x63, 0xFF, 0x00, 0x00}},
    {'F', {0x00, 0x00, 0xFF, 0x63, 0x60, 0x60, 0x64, 0x7C,
           0x64, 0x60, 0x60, 0x60, 0x60, 0xF0, 0x00, 0x00}},
    {'G', {0x00, 0x00, 0x3E, 0x63, 0xC3, 0xC0, 0xC0, 0xCF,
           0xC3, 0xC3, 0xC3, 0xC3, 0x63, 0x3E, 0x00, 0x00}},
    {'H', {0x00, 0x00, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0xFF,
           0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0x00, 0x00}},
    {'I', {0x00, 0x00, 0x7E, 0x18, 0x18, 0x18, 0x18, 0x18,
           0x18, 0x18, 0x18, 0x18, 0x18, 0x7E, 0x00, 0x00}},
    {'J', {0x00, 0x00, 0x1F, 0x06, 0x06, 0x06, 0x06, 0x06,
           0x06, 0x06, 0xC6, 0xC6, 0x6C, 0x38, 0x00, 0x00}},
    {'K', {0x00, 0x00, 0xE3, 0x66, 0x6C, 0x78, 0x70, 0x78,
           0x6C, 0x66, 0x63, 0x63, 0x63, 0xE3, 0x00, 0x00}},
    {'L', {0x00, 0x00, 0xF0, 0x60, 0x60, 0x60, 0x60, 0x60,
           0x60, 0x60, 0x60, 0x60, 0x63, 0xFF, 0x00, 0x00}},
    {'M', {0x00, 0x00, 0xC3, 0xE7, 0xFF, 0xDB, 0xDB, 0xC3,
           0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0x00, 0x00}},
    {'N', {0x00, 0x00, 0xC3, 0xE3, 0xF3, 0xFB, 0xDF, 0xCF,
           0xC7, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0x00, 0x00}},
    {'O', {0x00, 0x00, 0x3C, 0x66, 0xC3, 0xC3, 0xC3, 0xC3,
           0xC3, 0xC3, 0xC3, 0xC3, 0x66, 0x3C, 0x00, 0x00}},
    {'P', {0x00, 0x00, 0xFC, 0x66, 0x63, 0x63, 0x63, 0x66,
           0x7C, 0x60, 0x60, 0x60, 0x60, 0xF0, 0x00, 0x00}},
    {'Q', {0x00, 0x00, 0x3C, 0x66, 0xC3, 0xC3, 0xC3, 0xC3,
           0xC3, 0xC3, 0xDB, 0xCF, 0x66, 0x3F, 0x03, 0x00}},
    {'R', {0x00, 0x00, 0xFC, 0x66, 0x63, 0x63, 0x63, 0x66,
           0x7C, 0x6C, 0x66, 0x63, 0x63, 0xE3, 0x00, 0x00}},
    {'S', {0x00, 0x00, 0x7E, 0xC3, 0xC0, 0xC0, 0x60, 0x3C,
           0x06, 0x03, 0x03, 0x03, 0xC3, 0x7E, 0x00, 0x00}},
    {'T', {0x00, 0x00, 0xFF, 0xDB, 0x18, 0x18, 0x18, 0x18,
           0x18, 0x18, 0x18, 0x18, 0x18, 0x3C, 0x00, 0x00}},
    {'U', {0x00, 0x00, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3,
           0xC3, 0xC3, 0xC3, 0xC3, 0x66, 0x3C, 0x00, 0x00}},
    {'V', {0x00, 0x00, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0x66,
           0x66, 0x66, 0x3C, 0x3C, 0x18, 0x18, 0x00, 0x00}},
    {'W', {0x00, 0x00, 0xC3, 0xC3, 0xC3, 0xC3, 0xC3, 0xDB,
           0xDB, 0xDB, 0xFF, 0x66, 0x66, 0x66, 0x00, 0x00}},
    {'X', {0x00, 0x00, 0xC3, 0xC3, 0x66, 0x66, 0x3C, 0x18,
           0x18, 0x3C, 0x66, 0x66, 0xC3, 0xC3, 0x00, 0x00}},
    {'Y', {0x00, 0x00, 0xC3, 0xC3, 0x66, 0x66, 0x3C, 0x18,
           0x18, 0x18, 0x18, 0x18, 0x18, 0x3C, 0x00, 0x00}},
    {'Z', {0x00, 0x00, 0xFF, 0xC3, 0x06, 0x0C, 0x0C, 0x18,
           0x30, 0x30, 0x60, 0xC0, 0xC3, 0xFF, 0x00, 0x00}},
};

static HAL_StatusTypeDef screen_driver_write(screen_driver_t *driver,
                                             bool is_data, const uint8_t *bytes,
                                             uint16_t length);
static HAL_StatusTypeDef screen_driver_command(screen_driver_t *driver,
                                               uint8_t command);
static const uint8_t *screen_driver_find_glyph(char character);
static const uint8_t *screen_driver_find_large_glyph(char character);
static void screen_driver_delay(uint32_t milliseconds);

HAL_StatusTypeDef screen_driver_init(screen_driver_t *driver,
                                     const screen_driver_config_t *config) {
  static const uint8_t initialization_commands[] = {
      0x29U, /* 电源控制：电压转换器 */
      0x2BU, /* 电源控制：转换器和稳压器 */
      0x2FU, /* 电源控制：转换器、稳压器和跟随器 */
      0x24U, /* 电阻比 */
      0x81U, /* 电子音量命令 */
      0x20U, /* 电子音量值 */
      0xA2U, /* 1/9 偏压 */
      0xA0U, /* 段扫描方向 */
      0xC8U, /* COM 扫描方向 */
      0xAFU, /* 开启显示 */
      0xA4U, /* 正常显示显存内容 */
  };

  if ((driver == NULL) || (config == NULL) || (config->spi == NULL) ||
      (config->chip_select_port == NULL) ||
      (config->data_command_port == NULL) || (config->reset_port == NULL)) {
    return HAL_ERROR;
  }

  memset(driver, 0, sizeof(*driver));
  driver->config = *config;
  HAL_GPIO_WritePin(config->chip_select_port, config->chip_select_pin,
                    GPIO_PIN_SET);
  HAL_GPIO_WritePin(config->reset_port, config->reset_pin, GPIO_PIN_RESET);
  screen_driver_delay(100U);
  HAL_GPIO_WritePin(config->reset_port, config->reset_pin, GPIO_PIN_SET);
  screen_driver_delay(100U);
  /* 通过硬件引脚复位屏幕控制器。 */
  if (screen_driver_command(driver, 0xE2U) != HAL_OK) {
    return HAL_ERROR;
  }
  screen_driver_delay(10U);

  for (uint32_t command_index = 0U; command_index < sizeof(initialization_commands);
       ++command_index) {
    if (screen_driver_command(driver, initialization_commands[command_index]) !=
        HAL_OK) {
      return HAL_ERROR;
    }
    if (command_index == 2U) {
      screen_driver_delay(100U);
    } else {
      screen_driver_delay(10U);
    }
  }

  driver->initialized = true;
  screen_driver_clear(driver);
  return screen_driver_flush(driver);
}

void screen_driver_clear(screen_driver_t *driver) {
  if (driver == NULL) {
    return;
  }
  memset(driver->framebuffer, 0, sizeof(driver->framebuffer));
}

void screen_driver_fill_row(screen_driver_t *driver, uint8_t row, bool on) {
  if ((driver == NULL) || (row >= SCREEN_DRIVER_PAGE_COUNT)) {
    return;
  }
  memset(&driver->framebuffer[(uint16_t)row * SCREEN_DRIVER_WIDTH],
         on ? 0xFF : 0x00, SCREEN_DRIVER_WIDTH);
}

void screen_driver_draw_pixel(screen_driver_t *driver, uint8_t x, uint8_t y,
                              bool on) {
  uint16_t index;
  uint8_t mask;

  if ((driver == NULL) || (x >= SCREEN_DRIVER_WIDTH) ||
      (y >= SCREEN_DRIVER_HEIGHT)) {
    return;
  }
  index = ((uint16_t)(y / 8U) * SCREEN_DRIVER_WIDTH) + x;
  mask = (uint8_t)(UINT8_C(1) << (y & 7U));
  if (on) {
    driver->framebuffer[index] |= mask;
  } else {
    driver->framebuffer[index] &= (uint8_t)~mask;
  }
}

void screen_driver_draw_line(screen_driver_t *driver, uint8_t x0, uint8_t y0,
                             uint8_t x1, uint8_t y1, bool on) {
  int16_t x = x0;
  int16_t y = y0;
  const int16_t target_x = x1;
  const int16_t target_y = y1;
  const int16_t delta_x =
      (target_x >= x) ? (target_x - x) : (x - target_x);
  const int16_t step_x = (x < target_x) ? 1 : -1;
  const int16_t delta_y =
      (target_y >= y) ? (y - target_y) : (target_y - y);
  const int16_t step_y = (y < target_y) ? 1 : -1;
  int16_t error = delta_x + delta_y;

  for (;;) {
    screen_driver_draw_pixel(driver, (uint8_t)x, (uint8_t)y, on);
    if ((x == target_x) && (y == target_y)) {
      break;
    }
    {
      const int16_t doubled_error = (int16_t)(2 * error);

      if (doubled_error >= delta_y) {
        error = (int16_t)(error + delta_y);
        x = (int16_t)(x + step_x);
      }
      if (doubled_error <= delta_x) {
        error = (int16_t)(error + delta_x);
        y = (int16_t)(y + step_y);
      }
    }
  }
}

void screen_driver_draw_text(screen_driver_t *driver, uint8_t row,
                             uint8_t column, const char *text, bool inverse) {
  if ((driver == NULL) || (text == NULL) || (row >= SCREEN_DRIVER_PAGE_COUNT)) {
    return;
  }

  while ((*text != '\0') && ((uint16_t)column + SCREEN_DRIVER_CHARACTER_WIDTH <=
                             SCREEN_DRIVER_WIDTH)) {
    const uint8_t *glyph = screen_driver_find_glyph(*text);
    uint8_t glyph_column;
    uint16_t buffer_index = ((uint16_t)row * SCREEN_DRIVER_WIDTH) + column;

    for (glyph_column = 0U; glyph_column < SCREEN_DRIVER_GLYPH_WIDTH;
         ++glyph_column) {
      driver->framebuffer[buffer_index + glyph_column] =
          inverse ? (uint8_t)~glyph[glyph_column] : glyph[glyph_column];
    }
    driver->framebuffer[buffer_index + SCREEN_DRIVER_GLYPH_WIDTH] =
        inverse ? 0xFFU : 0x00U;

    column += SCREEN_DRIVER_CHARACTER_WIDTH;
    ++text;
  }
}

void screen_driver_draw_text_large(screen_driver_t *driver, uint8_t row,
                                   uint8_t column, const char *text,
                                   bool inverse) {
  if ((driver == NULL) || (text == NULL) ||
      (row >= SCREEN_DRIVER_LARGE_ROW_COUNT)) {
    return;
  }

  while ((*text != '\0') &&
         ((uint16_t)column + SCREEN_DRIVER_LARGE_CHARACTER_WIDTH <=
          SCREEN_DRIVER_WIDTH)) {
    const uint8_t *glyph = screen_driver_find_large_glyph(*text);
    const uint16_t upper_page_offset =
        ((uint16_t)row * 2U * SCREEN_DRIVER_WIDTH) + column;
    const uint16_t lower_page_offset =
        upper_page_offset + SCREEN_DRIVER_WIDTH;

    for (uint8_t glyph_column = 0U;
         glyph_column < SCREEN_DRIVER_LARGE_GLYPH_WIDTH; ++glyph_column) {
      uint16_t column_pixels = 0U;

      for (uint8_t glyph_row = 0U;
           glyph_row < SCREEN_DRIVER_LARGE_GLYPH_HEIGHT; ++glyph_row) {
        if ((glyph[glyph_row] & (uint8_t)(0x80U >> glyph_column)) != 0U) {
          column_pixels |= (uint16_t)(UINT16_C(1) << glyph_row);
        }
      }
      if (inverse) {
        column_pixels = (uint16_t)~column_pixels;
      }
      driver->framebuffer[upper_page_offset + glyph_column] =
          (uint8_t)(column_pixels & 0xFFU);
      driver->framebuffer[lower_page_offset + glyph_column] =
          (uint8_t)(column_pixels >> 8U);
    }

    column += SCREEN_DRIVER_LARGE_CHARACTER_WIDTH;
    ++text;
  }
}

HAL_StatusTypeDef screen_driver_flush(screen_driver_t *driver) {
  if ((driver == NULL) || !driver->initialized) {
    return HAL_ERROR;
  }

  for (uint8_t page = 0U; page < SCREEN_DRIVER_PAGE_COUNT; ++page) {
    const uint8_t page_commands[] = {
        (uint8_t)(0xB0U | page),
        0x10U,
        0x00U,
    };
    if (screen_driver_write(driver, false, page_commands,
                            sizeof(page_commands)) != HAL_OK) {
      return HAL_ERROR;
    }
    if (screen_driver_write(
            driver, true,
            &driver->framebuffer[(uint16_t)page * SCREEN_DRIVER_WIDTH],
            SCREEN_DRIVER_WIDTH) != HAL_OK) {
      return HAL_ERROR;
    }
  }
  return HAL_OK;
}

static HAL_StatusTypeDef screen_driver_write(screen_driver_t *driver,
                                             bool is_data, const uint8_t *bytes,
                                             uint16_t length) {
  HAL_StatusTypeDef status;

  if ((driver == NULL) || (bytes == NULL) || (length == 0U)) {
    return HAL_ERROR;
  }

  HAL_GPIO_WritePin(driver->config.data_command_port,
                    driver->config.data_command_pin,
                    is_data ? GPIO_PIN_SET : GPIO_PIN_RESET);
  HAL_GPIO_WritePin(driver->config.chip_select_port,
                    driver->config.chip_select_pin, GPIO_PIN_RESET);
  status = HAL_SPI_Transmit(driver->config.spi, (uint8_t *)bytes, length,
                            SCREEN_DRIVER_SPI_TIMEOUT_MS);
  HAL_GPIO_WritePin(driver->config.chip_select_port,
                    driver->config.chip_select_pin, GPIO_PIN_SET);
  return status;
}

static HAL_StatusTypeDef screen_driver_command(screen_driver_t *driver,
                                               uint8_t command) {
  return screen_driver_write(driver, false, &command, 1U);
}

static const uint8_t *screen_driver_find_glyph(char character) {
  uint32_t glyph_index;
  const uint32_t glyph_count = sizeof(screen_font) / sizeof(screen_font[0]);

  if ((character >= 'a') && (character <= 'z')) {
    character = (char)(character - ('a' - 'A'));
  }
  for (glyph_index = 0U; glyph_index < glyph_count; ++glyph_index) {
    if (screen_font[glyph_index].character == character) {
      return screen_font[glyph_index].columns;
    }
  }

  /* 字符 '?' 位于字模表第 17 项，用作不支持字符的替代字形。 */
  return screen_font[17].columns;
}

static const uint8_t *screen_driver_find_large_glyph(char character) {
  const uint32_t glyph_count =
      sizeof(screen_large_font) / sizeof(screen_large_font[0]);

  if ((character >= 'a') && (character <= 'z')) {
    character = (char)(character - ('a' - 'A'));
  }
  for (uint32_t glyph_index = 0U; glyph_index < glyph_count; ++glyph_index) {
    if (screen_large_font[glyph_index].character == character) {
      return screen_large_font[glyph_index].rows;
    }
  }

  return screen_large_font[17].rows;
}

static void screen_driver_delay(uint32_t milliseconds) {
  TickType_t ticks = pdMS_TO_TICKS(milliseconds);
  vTaskDelay((ticks == 0U) ? 1U : ticks);
}
